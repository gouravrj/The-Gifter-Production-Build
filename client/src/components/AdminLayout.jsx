import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { Boxes, ClipboardList, LayoutDashboard, LogOut, Menu, Sparkles, Store, Users, X } from 'lucide-react';
import { useState } from 'react';
import { useAuth } from '../context/AuthContext';
const nav=[['Overview','/admin/dashboard',LayoutDashboard],['Products','/admin/products',Boxes],['Orders','/admin/orders',ClipboardList],['Custom requests','/admin/customizations',Sparkles],['Users','/admin/users',Users]];
export default function AdminLayout(){const [open,setOpen]=useState(false);const {logout}=useAuth();const navigate=useNavigate();return <div className="min-h-screen bg-[#f5f3ef]"><aside className={`fixed inset-y-0 left-0 z-50 w-72 bg-ink p-6 text-white transition lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between"><span className="font-display text-2xl">The Gifter<span className="text-clay">.</span></span><button className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><p className="mt-1 text-xs uppercase tracking-[.25em] text-white/40">Studio admin</p><nav className="mt-10 grid gap-2">{nav.map(([label,to,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold transition ${isActive?'bg-clay text-white':'text-white/65 hover:bg-white/10 hover:text-white'}`}><Icon size={19}/>{label}</NavLink>)}</nav><button onClick={()=>{logout();navigate('/admin')}} className="absolute bottom-6 left-6 flex items-center gap-3 text-sm text-white/60 hover:text-white"><LogOut size={18}/>Sign out</button></aside><div className="lg:pl-72"><header className="flex h-20 items-center justify-between border-b border-sand/40 bg-white px-5 sm:px-8"><button className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><p className="hidden text-sm text-ink/50 sm:block">Handmade business command centre</p><a href="/" className="flex items-center gap-2 text-sm font-semibold"><Store size={18}/>View store</a></header><main className="p-5 sm:p-8"><Outlet/></main></div>{open&&<button aria-label="Close menu" onClick={()=>setOpen(false)} className="fixed inset-0 z-40 bg-ink/50 lg:hidden"/>}</div>}
