const steps = ['pending', 'shipped', 'delivered'];
export default function OrderTimeline({ status }) { const active = steps.indexOf(status); return <div className="mt-5 flex items-center">{steps.map((step, i)=><div key={step} className={`flex items-center ${i < steps.length - 1 ? 'flex-1' : ''}`}><div className="flex flex-col items-center"><span className={`grid h-8 w-8 place-items-center rounded-full text-sm font-bold ${i <= active ? 'bg-sage text-white' : 'bg-sand/40'}`}>{i+1}</span><span className="mt-2 text-xs capitalize">{step}</span></div>{i < steps.length - 1 && <div className={`mb-5 h-1 flex-1 ${i < active ? 'bg-sage' : 'bg-sand/40'}`}/>}</div>)}</div>; }
