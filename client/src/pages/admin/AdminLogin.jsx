import { useState } from 'react';
import { Navigate, useNavigate } from 'react-router-dom';
import toast from 'react-hot-toast';
import api, { errorMessage } from '../../services/api';
import { useAuth } from '../../context/AuthContext';
export default function AdminLogin(){const {user,saveSession}=useAuth();const nav=useNavigate();const [form,setForm]=useState({email:'',password:''});const [busy,setBusy]=useState(false);if(user?.role==='admin')return <Navigate to="/admin/dashboard" replace/>;const submit=async e=>{e.preventDefault();setBusy(true);try{const {data}=await api.post('/auth/admin-login',form);saveSession(data);nav('/admin/dashboard')}catch(err){toast.error(errorMessage(err))}finally{setBusy(false)}};return <div className="grid min-h-screen place-items-center bg-ink p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-cream p-8 shadow-2xl"><p className="text-sm font-bold uppercase tracking-[.25em] text-clay">Private studio</p><h1 className="mt-2 text-4xl">Admin access</h1><p className="mt-2 text-sm text-ink/55">Authorized team members only.</p><div className="mt-7 grid gap-4"><div><label className="label">Admin email</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div><label className="label">Password</label><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><button disabled={busy} className="btn-primary mt-2">{busy?'Checking…':'Enter dashboard'}</button></div></form></div>}
