import { useEffect, useState } from 'react';
import toast from 'react-hot-toast';
import api, { errorMessage } from '../../services/api';
import EmptyState from '../../components/EmptyState';
export default function Users(){const [users,setUsers]=useState([]);useEffect(()=>{api.get('/admin/users').then(r=>setUsers(r.data.users)).catch(e=>toast.error(errorMessage(e)))},[]);return <><p className="text-sm font-bold uppercase tracking-widest text-clay">Community</p><h1 className="mt-2 text-4xl">Customers</h1>{!users.length?<div className="mt-8"><EmptyState title="No customers yet"/></div>:<div className="card mt-8 overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-sand/50 bg-cream"><tr>{['Name','Email','Saved address','Joined'].map(h=><th key={h} className="px-5 py-4">{h}</th>)}</tr></thead><tbody>{users.map(u=><tr key={u._id} className="border-b border-sand/30"><td className="px-5 py-4 font-semibold">{u.fullName}</td><td className="px-5 py-4">{u.email}</td><td className="max-w-xs truncate px-5 py-4">{u.address}</td><td className="px-5 py-4">{new Date(u.createdAt).toLocaleDateString('en-IN')}</td></tr>)}</tbody></table></div>}</>}
